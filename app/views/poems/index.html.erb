<% @rounds.each do |round| %>
	<h3><%= round.title %></h3>
	<ul>
	<% if round.poems.empty? %>
		<li>There are no poets in this round yet</li>
	<% else %>
		<% round.poems.each do |poem| %>
		<li><%=link_to "#{User.find(poem.user_id).username}", poem_path(poem) %></li>
		<% end %>
	<% end %>
	</ul>
<% end %>
<br>
<%= link_to "Home", '/player' %>
