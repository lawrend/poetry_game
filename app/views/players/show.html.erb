<p>Hello, <%=@player.username%>.</p>
<hr>
<p>You are currently participating in <%= @player.current_rounds %> rounds and your maximum is set to <%= @player.max_rounds %> round(s). </p>
<ul>
  <li><%= form_tag("/players/#{@player.id}", method: "patch") do %>
  <%= label_tag(:player, "Maximum Rounds") %>
  <%= number_field(:player, :max_rounds) %>
  <%= submit_tag("Update") %>
  <% end %>
    </li>
  <li><%= link_to "Create A New Round", new_round_path %></li>
  <li><%= link_to "View Current/Past Rounds", rounds_path%></li>
  <li><%= link_to "View Poems", poems_path %></li>
</ul>
<br>
<br>

<% if @player.eligible? %>
You can write some verses.
<% else %>
Your maximum number of poems has been reached!
<%end%><br>
<h3>Invite some of these poets</h3>
<ul>
<% User.possible_players.each do |player| %>
  <% if player.username != @player.username %>
    <li><%= player.username %><br></li>
  <% end %>
<% end %>
</ul>

